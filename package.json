{
  "name": "landingpageforfamilyacadamy",
  "description": "Public cinematic landing page and download hub for Family Academy.",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "python3 -m http.server 3000 --bind 127.0.0.1",
    "serve": "python3 -m http.server 3000 --bind 127.0.0.1",
    "build": "echo 'Static site - no build step required'"
  }
}
